<?php
/*
 * dbbhInstaller sample configuration file.
 * The parameters may have default values. If so they will be typed in the input elements.
 * The comment (//) after the parameter is the name for the parameneter at the installation script.
 * The comment (//) without parameter is the section header.
 * This is a sample configuration file. These first parameters must exist to connect to the database.
 *
 * To run the installation script - execute install.php
*/

// Database connection parameters
$db_host = '';      // Database server address
$db_user = '';      // Database server username
$db_password = '';  // Database server password
$db_database = '';  // Database name
$db_prefix = '';    // Database tables prefix
?>
